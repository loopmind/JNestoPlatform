<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <parent>
    <artifactId>jnesto-platform</artifactId>
    <groupId>com.jnesto</groupId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.jnesto.platform</groupId>
  <artifactId>MainFramePlugin</artifactId>
  <build>
    <resources>
      <resource>
        <directory>src/main/java</directory>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.6</version>
        <executions>
          <execution>
            <id>unzip jar file</id>
            <phase>package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <unzip />
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.3</version>
        <configuration>
          <descriptors>
            <descriptor>src/main/assembly/assembly.xml</descriptor>
          </descriptors>
          <appendAssemblyId>false</appendAssemblyId>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <archive>
            <manifestEntries>
              <plugin-id>${plugin.id}</plugin-id>
              <plugin-class>${plugin.class}</plugin-class>
              <plugin-version>${plugin.version}</plugin-version>
              <plugin-provider>${plugin.provider}</plugin-provider>
              <plugin-dependencies>${plugin.dependencies}</plugin-dependencies>
              <plugin-description>${plugin.description}</plugin-description>
              <plugin-requires>${plugin.requires}</plugin-requires>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>2.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>com.jnesto</groupId>
      <artifactId>jnesto-platform-core</artifactId>
      <version>0.0.1-SNAPSHOT</version>
      <scope>provided</scope>
      <exclusions>
        <exclusion>
          <artifactId>pf4j</artifactId>
          <groupId>ro.fortsoft.pf4j</groupId>
        </exclusion>
        <exclusion>
          <artifactId>beansbinding</artifactId>
          <groupId>org.jdesktop</groupId>
        </exclusion>
      </exclusions>
    </dependency>
  </dependencies>
  <properties>
    <plugin.class>com.jnesto.platform.demo.mainframe.MainFramePlugin</plugin.class>
    <plugin.version>${project.version}</plugin.version>
    <plugin.id>${project.artifactId}</plugin.id>
    <maven.compiler.source>1.8</maven.compiler.source>
    <plugin.provider>com.jnesto</plugin.provider>
    <maven.compiler.target>1.8</maven.compiler.target>
    <plugin.description>${project.description}</plugin.description>
  </properties>
</project>

